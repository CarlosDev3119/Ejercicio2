export interface calculadoraInterface {
    sumar(number1: number, number2: number): number;
    restar(number1: number, number2: number): number;
    multiplicar(number1: number, number2: number): number;
    dividir(number1: number, number2: number): number|string;
}
